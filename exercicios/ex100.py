#!/usr/bin/python36
from random import sample

def sorteia(lista):
    lista = sample(range(1, 11), k=5)
    for I in lista:
        numeros.append(I)    

def somapar():
    for n in numeros:
        if n % 2 == 0:
            
numeros = []
sorteia(numeros)
